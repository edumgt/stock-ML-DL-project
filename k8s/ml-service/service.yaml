apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: stock-mldl
  labels:
    app: ml-service
    app.kubernetes.io/part-of: stock-mldl
spec:
  type: ClusterIP
  selector:
    app: ml-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
