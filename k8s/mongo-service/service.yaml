apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  namespace: stock-mldl
  labels:
    app: mongo-service
    app.kubernetes.io/part-of: stock-mldl
spec:
  type: ClusterIP
  selector:
    app: mongo-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
